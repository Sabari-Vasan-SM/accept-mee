interface:
  display_name: "Accept Mee"
  short_description: "Repo-specific patterns and workflows for accept-mee"
  default_prompt: "Use the accept-mee repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true